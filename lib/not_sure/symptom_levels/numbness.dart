import 'package:flutter/material.dart';

class Symptom9Screen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Numbness or tingling in the feet'),
      ),
      body: Center(
        child: Text(
          'Details and method to check Numbness or tingling in the feet',
          style: TextStyle(fontSize: 18),
          textAlign: TextAlign.center,
        ),
      ),
    );
  }
}