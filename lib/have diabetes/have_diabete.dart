import 'package:flutter/material.dart';

class HaveDiabeteDashboard extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("Diabetes Dashboard")),
      body: Center(child: Text("Welcome to the Diabetes Dashboard")),
    );
  }
}
